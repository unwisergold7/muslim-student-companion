/**
 * The app uses the ISNA calculation method only.
 * There is no user-facing selector — if we ever add more methods,
 * this file is the single source of truth.
 */
export const DEFAULT_METHOD = 2; // AlAdhan API method id for ISNA
export const METHOD_NAME = "Islamic Society of North America (ISNA)";
